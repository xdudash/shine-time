# Focused application audit — 2026-09-14

Fixed:
- Cleaner job/history responses now strip entry instructions, key instructions, Wi-Fi, storage locations and object notes unless the job status is ACCEPTED, EN_ROUTE, ARRIVED or CLEANING. Earnings/history remain available. Existing assignment authorization is unchanged.
- Query cache discards previous data when refresh returns 401, 403 or 404. Revoked access must not leave cached job details visible after the response.
- Monthly settlement conflict clears the outdated selection, refreshes balances, and retains the error message in the mounted payment form. Administrator must review and select again; no automatic payment retry.
- Property gallery clamps its page after deletions, resets pagination on a route change, and clears old photo content and errors when the photo target changes.

Verification: TypeScript check and production build passed; all 68 local tests passed, including the full booking-to-settlement database workflow, a 1000-job batch and five new private-instruction regressions. No production business records changed by testing. Browser interaction and full production load testing were not performed; this is not a guarantee of absence of all bugs.

Deployment: st-api backend plus frontend archive Shine_Time_audit_update.zip; extract contents into public_html/app. Previous monthly finance and private property photo features are included. No database migration required for these fixes.

import {useEffect,useSyncExternalStore,useState,useCallback,useRef} from 'react';import {api} from './client';
type Entry={data?:unknown;error?:Error;loading:boolean;version:number};const cache=new Map<string,Entry>(),listeners=new Set<()=>void>(),flights=new Map<string,Promise<void>>();let generation=0;const emit=()=>listeners.forEach(f=>f());
export function clearQueries(){generation++;cache.clear();flights.clear();emit()}
export function invalidate(){for(const[k,e]of cache)cache.set(k,{...e,version:e.version+1});emit()}
export function useQuery<T>(route:string|null,query?:Record<string,string|number>){const key=JSON.stringify([route,query]);const subscribe=useCallback((f:()=>void)=>{listeners.add(f);return()=>{listeners.delete(f)}},[]);const entry=useSyncExternalStore(subscribe,()=>cache.get(key));const load=useCallback(()=>{if(!route)return Promise.resolve();if(flights.has(key))return flights.get(key)!;const epoch=generation,version=cache.get(key)?.version||0;cache.set(key,{...cache.get(key),loading:true,error:undefined,version});emit();const promise=api<T>(route,{query}).then(data=>{if(epoch===generation)cache.set(key,{data,loading:false,version:cache.get(key)?.version||0})}).catch(error=>{if(epoch===generation)cache.set(key,{...cache.get(key),loading:false,error,version:cache.get(key)?.version||0})}).finally(()=>{if(flights.get(key)===promise){flights.delete(key);emit();if(epoch===generation&&(cache.get(key)?.version||0)>version)void load()}});flights.set(key,promise);return promise;},[route,key]);useEffect(()=>{void load()},[load,entry?.version??0]);return{data:entry?.data as T|undefined,error:entry?.error,loading:!!route&&(!entry||entry.loading&&entry.data===undefined),reload:load};}
export function useMutation(){const[busy,setBusy]=useState(false),[error,setError]=useState(''),[done,setDone]=useState(false),lock=useRef(false);async function run(fn:()=>Promise<unknown>){if(lock.current)return;lock.current=true;setBusy(true);setError('');setDone(false);try{await fn();invalidate();setDone(true)}catch(e){setError(e instanceof Error?e.message:String(e))}finally{lock.current=false;setBusy(false)}}return{busy,error,done,run};}
